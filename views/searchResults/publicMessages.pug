for m in publicMessages
    .messageBox
        .senderInfo
            span.serderName #{m.senderName}
            i.statusIcon(class=m.userStatus)
        .messageContent
            span #{m.content}
            span.timestamp #{m.createdAt.toLocaleString()}